# graphqldm/constants.py

# Define GraphQL default endpoint URL and output format
DEFAULT_GRAPHQL_ENDPOINT = "https://api.example.com/graphql"
DEFAULT_OUTPUT_FORMAT = "json"
DEFAULT_OUTPUT_FILE = "result.json"
OUTPUT_FILE_FORMAT = {
    "json" : "result.json",
    "table": "result.txt",
    "yaml": "result.yaml"
}